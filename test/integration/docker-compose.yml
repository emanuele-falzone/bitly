version: '3.1'

services:

  mongo:
    image: mongo:4.4.14
    hostname: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    ports:
      - 27017:27017

  redis:
    image: redis:6.2.7-alpine
    hostname: redis
    ports:
      - 6379:6379